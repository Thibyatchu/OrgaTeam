{% extends 'base.html.twig' %}

{% block title %}Hello EvenementController!{% endblock %}

{% block body %}
<link rel="stylesheet" href="{{ asset('teamtheme/assets/css/style.css') }}">

<div class="container-scroller">
    <div class="example-wrapper">
        <h1>Hello in Evenement ! </h1>

        <ul>
        {% for evenement in evenements %}
            <h3>{{ evenement.Nom}} - {{ evenement.evenements.nom|default('na')}}</h3>
        {% endfor %}
        </ul>
        <a href="evenement/add" class="btn btn-primary">Ajouter un évènement</a>
    </div>
</div>
{% endblock %}
